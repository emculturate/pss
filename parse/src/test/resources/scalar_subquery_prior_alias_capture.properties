orderedAliasFromScalarSubqueryInArithmeticConsumerTest=SELECT (SELECT max(x) FROM t2) AS prior_alias, prior_alias + 1 AS nxt FROM t1
orderedAliasFromScalarSubqueryInWindowPartitionByTest=SELECT (SELECT max(x) FROM t2) AS prior_alias, ROW_NUMBER() OVER (PARTITION BY prior_alias) AS rn FROM t1
